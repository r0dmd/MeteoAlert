import { useDocumentTitle } from '../../hooks/index.js';

// ------------------------------------------
const LoginPage = () => {
  useDocumentTitle('Iniciar sesión');

  return <p>test</p>;
};

export default LoginPage;
